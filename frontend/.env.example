# Server-side only: where /api/* is proxied to (your backend origin)
API_PROXY_URL=http://localhost:4000
# Browser: Socket.io connects here directly
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000
# Browser: canonical app URL (used for share links / metadata)
NEXT_PUBLIC_APP_URL=http://localhost:3000
